# ntquant parameter scan configuration — TEMPLATE
# 使用方式：拷贝为 configs/param.yaml 后按需修改（用户副本已被 .gitignore 忽略）。
#   cp configs/param.example.yaml configs/param.yaml
# Scan grids over strategy parameters.
scan:
  fast_period: [5, 10, 15, 20]
  slow_period: [30, 50, 100]
  trade_size: ["10000", "20000"]

data:
  instrument_id: EUR/USD.SIM
  count: 2000
  seed: 1234
  catalog_path: docs/data
  bar_type: EUR/USD.SIM-1-MINUTE-LAST-EXTERNAL
  source: synthetic

output_path: output
log_level: WARNING
